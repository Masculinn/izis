import { DiscoverContentProps } from "@/interfaces";
import { FC } from "react";

const Content: FC<DiscoverContentProps> = ({ desc }) => {
  return (
    <section className="lg:py-24 py-12 max-w-7xl mx-auto">
      <p></p>
    </section>
  );
};

export default Content;
